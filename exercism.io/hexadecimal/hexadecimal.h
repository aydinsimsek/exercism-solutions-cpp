#if !defined(HEXADECIMAL_H)
#define HEXADECIMAL_H

#include <map> 
#include <string>
using namespace std; 

namespace hexadecimal {
	int convert(string num); 
}  // namespace hexadecimal

#endif // HEXADECIMAL_H